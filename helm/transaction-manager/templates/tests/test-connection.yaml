apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transaction-manager.fullname" . }}-test-connection"
  labels:
    {{- include "transaction-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transaction-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
